import React from 'react';

function HomePage() {
  // ()'s to make this single expression
  return (
    <div className="jumbotron">
      <h1>Admin...</h1>
      <p>React, Flux, Router, etc...</p>
    </div>
  );
}

// single item, default export
export default HomePage;
